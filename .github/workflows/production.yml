name: Production backend


on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy
    runs-on: self-hosted
    steps:
      - uses: dtolnay/rust-toolchain@stable
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run server
        run: cargo run